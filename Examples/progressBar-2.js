// import "nprogress/nprogress.css"; // in 'main.js'

//All code below in 'EventShow.js'

/*import store from "@/store/index";
import store from "@/store/index";

props: ["id"],
  beforeRouteEnter(to, from, next) {
    NProgress.start(); //start the progress before navigating to this component
    store.dispatch("event/fetchEvent", to.params.id).then(() => {
      NProgress.done();
      next();
    });
  }
  //Add 'return EventService' so 'then' /above/ can work
  */
